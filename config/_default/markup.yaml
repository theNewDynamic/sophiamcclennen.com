# Markup
# We assume we're using Goldmark
# https://gohugo.io/getting-started/configuration-markup#readout
# ################################
highlight:
  style: monokai
  noClasses: false
goldmark:
  renderer:
    unsafe: true