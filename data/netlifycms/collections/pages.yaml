name: "pages"
label: "Pages"
label_singular: "Pages"
folder: "content/en/"
filter: { field: "exclude_collection" }
create: true
sortableFields: ['title']
preview_path: /{{slug}}
fields:
  - import field title
  - import field date
  - import field description
  - import field body
  - import field seo


